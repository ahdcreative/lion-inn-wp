SELECT id, name, toPublish FROM prefixplaceholder_menu WHERE where_placeholder_field = where_placeholder_value;
-- fk_placeholder is not a Foreign Key in this, 
-- but it works better to keep it like this in the code